describe('The counter function', function(){

    it('should push names greeted into an array and count names pushed', function(){

        assert.equal(counter ('James'), countGreet = 1);
      });

    });
